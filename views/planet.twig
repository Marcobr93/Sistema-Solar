{% extends "layout.twig" %}

{% block content %}
    <div class="container">
        <h1>{{ planet.nombrePlaneta }}</h1>
        <hr>
        <h2>{{ planet.imagen }}</h2>
        <hr>
        <h2>{{ planet.simbolo }}</h2>
        <p class="col-lg-12">{{ planet.inclinacionOrbital }}</p>
    </div>
{% endblock %}